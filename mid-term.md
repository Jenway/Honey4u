
## 一、课题主要任务

针对基于异步公共子集（ACS）的拜占庭容错（BFT）共识协议中，因网络延迟导致部分诚实节点的广播数据被丢弃，进而造成带宽严重浪费的问题，本文设计并实现了一种高效的广播数据跨轮次复用机制。本研究属于异步BFT共识领域，其共识目标是使各节点对同一轮最终提交的交易集合达成一致。拜占庭容错要求协议在部分节点任意作恶的情况下仍能保持安全性，而异步模型则强调协议不依赖同步假设或超时正确性。

从异步BFT共识协议的研究现状来看，当前主要存在两类架构。第一类是基于ACS的架构，典型代表包括HoneyBadger和Dumbo。在这类架构中，广播与共识基本串行执行，广播阶段的冗余数据更容易造成带宽浪费。第二类是基于有向无环图的架构，如Narwhal配合Tusk或Bullshark，其特点是数据传播与共识并行或解耦，能够在一定程度上绕开广播浪费问题。本课题所关注的是经典ACS架构下的广播复用问题。

异步公共子集ACS的典型流程是：广播加共识结构。每个节点先提出一批交易，广播阶段负责保证各节点提案的可用性与一致性，共识阶段则从所有提案中确定一个公共子集。基于ACS的BFT协议会将该公共子集对应的交易集合组装并上链。然而，ACS中的广播浪费问题较为突出。在一轮ACS中，各节点都会广播自己的交易批次。由于异步BFT共识不依赖时间假设，无法等待全部N个节点都及时完成广播，典型的ACS只需确定一个包含至少N减f个提案的公共子集即可推进当前轮。因此，部分诚实节点发出的、但因网络延迟而到达较晚的广播结果很可能无法进入本轮的最终集合。这些已经传播但未被实际采用的数据，就构成了广播带宽的浪费。

为解决上述问题，本课题提出一种跨轮次的数据索引与回收策略。具体而言，在当前轮次共识结束后，并不立即丢弃那些未进入共识集合但内容有效的广播数据，而是将其缓存起来。在下一轮共识的提案中，通过轻量级索引对这些数据进行延迟打包或跨轮次复用，从而避免重复传输相同的数据内容，有效减少带宽消耗。


## 二、简述开题以来所做的具体工作和取得的进展

开题以来，围绕基于异步公共子集的拜占庭容错共识协议中的广播浪费问题，开展了系统性的研究与实践工作，主要包括以下几个方面的内容。

首先，在文献调研与理论准备方面，阅读并系统梳理了异步BFT、异步公共子集以及HoneyBadger、Dumbo系列相关的核心论文，明确了课题的研究背景、技术难点与整体技术路线，为后续的方案设计奠定了理论基础。

其次，在机制设计与实现方面，完成了广播数据跨轮次复用机制的基本方案设计与初步实现。该复用机制具体包含三个核心环节：第一是截留缓存机制，当共识选出 N-f 个有效负载后，继续等待一段特定时间，将在此期间完成但未被本轮选中的可靠广播输出存入本地缓存池，并为每个缓存条目计算轻量化的负载标识，即Payload ID，其计算方式为哈希函数作用于轮次、发送者以及数据哈希三者的组合。第二是重发提案机制，在准备发起下一轮提议时，节点会查看本地缓存池，取出可复用的条目作为新提案的一部分，提案中包含轻量化的引用即Payload ID以及对应的可靠广播证明，用于证明该广播数据已在网络中广泛可用。第三是数据还原机制，对于包含引用的共识提案，节点首先在本地内存池中查找对应的数据，若查找不到，则依据可靠广播证明从其他持有该数据的节点处下载。该复用机制支持跨轮次复用，并允许在获取数据后递归查找，从而进一步提升复用效率。

最后，在实验验证方面，搭建了本地多进程TCP网络模拟环境，对所提出的广播复用机制进行了初步验证。在吞吐量效果评估中，以端到端的物理吞吐量TPS作为主要性能指标，比较了关闭与开启广播复用机制时的性能差异。实验在相同参数设置下进行，即节点数N=12、拜占庭节点数f=3。结果表明，随着批处理尺寸的增大，系统吞吐量整体呈上升趋势。开启广播复用后，整条吞吐量曲线始终高于未开启复用的基线，说明跨轮复用能够有效减少重复广播所带来的带宽开销。在中高负载区间，物理吞吐量的提升幅度大致在18%至34%之间。例如，当批处理尺寸为8192时，吞吐量由约2226 TPS提升至约2628 TPS。


## 三、存在的主要问题


当前实验验证的范围仍然有限，尚未在更大规模的节点数量和更广的参数区间下进行系统性的测试。与此同时，对于不同批处理大小、节点规模以及网络扰动条件下的性能变化规律，目前还缺乏充分的分析。此外，广播复用机制在复杂网络条件下的稳定性与收益，也需要进一步的验证。

## 四、下一步的主要研究任务、具体设想与安排

下一步计划在更大规模的节点数量下开展测试，以进一步观察该机制的性能表现。同时，将扩展实验范围，覆盖更多的节点规模与批处理参数组合。在网络环境方面，准备引入更多的不确定性因素，从而验证机制在复杂条件下的有效性与稳定性。最后，将结合实验所获得的结果，对机制的设计与实现细节进行进一步优化。
