= 跨轮次复用机制的设计

== 系统模型与设计目标

我们采用经典的异步 BFT 系统模型：系统共有 $N$ 个节点，其中至多 $f$ 个拜占庭节点，满足 $N >= 3f+1$。网络完全异步，消息延迟无上界。每一轮协议中，各节点向系统广播一个包含若干待提交交易的批次，ACS 核心负责从中选出一个不小于 $N-f$ 个元素的公共子集并输出。

带宽浪费的根源在于广播与决定之间的时序错位。以 Dumbo 风格的 ACS 为例，节点在本地已收到 $N-f$ 个广播完成信号后便推进 MVBA 阶段，此时其余若干广播实例——即便它们全部来自诚实节点——仍可能未被本轮决定采纳。

设 $B_"broadcast"$ 为一轮内全部广播总字节数，$B_"unused"$ 为已完成可靠传播却未进入最终子集的广播字节量，则浪费比例为

$
  eta = B_"unused" / B_"broadcast".
$

$eta$ 并非常数。在网络延迟均匀的理想条件下，所有诚实节点的广播可能恰好同时完成，此时 $eta approx 0$（需要说明的是，这只对 HoneyBadger 这种 BKR94 风格的 ACS 成立）；而当网络存在不对称延迟，或若干诚实节点出口带宽受限时，$eta$ 可能接近 $f/N$。本文的目标是在不修改 ACS 黑盒决定语义的前提下，通过跨轮次复用将其显著降低。为此，机制需要满足三个基本要求：把未入选但已具备强可用性保证的广播结果转化为后续轮次的候选输入；确保这种转化不引入新的安全风险；把元数据、缓存维护和回取路径带来的额外开销控制在可接受范围内。

== 可用性前提与复用对象的界定

并非所有历史广播结果都可以作为复用对象。若缓存中存放的只是"本地节点曾经收到过、但无法证明其他节点也能恢复"的临时消息，则后续轮次中的轻量引用就可能使正确性问题从"是否应当采纳该广播结果"转化为"是否真的有人还能找回它"，复用本身反而成为新的安全风险。

不同协议对广播阶段的可用性保证有所不同。经典 RBC 保证了消息的最终一致投递，但不产生一个可以在协议外部独立验证的凭证。PRBC 在 RBC 基础上引入阈值签名或类似机制，使广播完成后能够产生公开可验证的证明材料，任意节点拿到这份证明后无需重新运行广播协议即可确认该数据曾经满足可用性条件。这一区别对本机制至关重要：复用依赖的正是这种外部可验证的凭证，而不仅仅是本地的广播完成状态。

因此，本机制要求被跨轮引用的广播结果在写入缓存时已经携带足够强的可用性证明，使其他诚实节点在后续轮次中能够独立验证该数据的可恢复性，并在必要时发起回取。对于采用纯 RBC 而没有额外可用性凭证的协议路径，除非另行引入等价的证明机制，否则本机制不能直接套用。这种机制依赖于广播阶段能够保证数据广泛可用，也就是类似于 PRBC 的机制。对于像 HoneyBadger、FIN 这种广播阶段使用的是 RBC 的协议来说，需要将广播阶段改为 PRBC。

== 条目标识与证明绑定

要让历史广播结果能够在后续轮次中被安全引用，首先必须为其建立稳定、可验证的身份。每个可复用条目记录以下字段：来源轮次、来源发送者、广播根哈希、证明材料，以及用于跨轮查询的轻量标识 `item_id`。

`item_id` 本身不承担安全性，而是用作缓存命中和跨轮索引的快速键。真正保证引用可信的，是 `item_id` 与来源信息、根哈希和证明材料之间的绑定关系。若仅使用本地递增编号或未绑定来源的摘要作为引用标识，其他节点即使收到了引用，也无法判断它究竟指向哪一轮、哪个发送者以及哪份可用广播结果，更无法在缓存未命中时确定向谁发起回取。

在提案编码层面，本机制采用"内联负载 + 引用集合"的统一格式：一个 ACS 提案既可以直接携带本轮新增的交易批次，也可以携带若干来自历史广播结果的轻量引用，两者可以同时出现在同一提案中。这样设计的好处是复用机制作为协议输入的一部分出现，而不是外部旁路逻辑，且 Python 宿主与 Rust 宿主可以共享同一线格式，降低跨语言边界的额外复杂度。

== 生命周期管理

复用机制有效的关键不只在于"把数据存起来"，而在于明确定义何时写入、何时重提、何时删除。

一轮 ACS 决定结束后，节点并不立刻丢弃所有未入选广播，而是在一个有限的等待窗口（Grace 窗口）内继续观察迟到但有效的广播输出。只有满足可用性条件且落在这一窗口内的广播结果，才会被写入复用池。Grace 窗口的长度需要与网络条件共同调谐：窗口过短则截留不到足够的迟到输出；窗口过长则每轮结束后的等待时间过大，抵消复用收益。

条目在生命周期内经历四个阶段：写入缓存、可重提、已消费、过期删除。写入缓存表示条目已满足保留条件；可重提表示它尚未在后续轮次中被成功消费，且仍位于生命周期窗口内；已消费表示它已经通过后续提案真正进入了某轮决定结果；过期删除对应超过最大存活轮数或因容量限制被清理的情况。

在构造下一轮提案时，节点依据每轮重提上限参数对缓存条目进行裁剪，而不是无上限地重提所有历史条目。若不加限制，复用本身就会演化成新的通信膨胀；若限制过于严格，迟到结果又无法被真正消费。消费标记和去重检查是重提策略的必要组成部分，确保同一历史条目不会在多个后续轮次中被反复引用。

== 机制执行流程

复用机制的完整执行流程分为四步。

第一步，一轮 ACS 决定结束后，节点在 Grace 窗口内继续观察迟到但满足可用性条件的广播输出，将其写入本地复用池。

第二步，下一轮准备提案时，节点从复用池中筛选若干尚未过期且尚未被消费的条目，将它们编码为引用集合并附着到新的 ACS 提案中。若本轮自身仍有新交易，也可以一起作为内联负载发送。

第三步，其他节点收到带引用的提案后，先根据 `item_id` 查询本地缓存。若命中，则直接将引用还原为历史负载参与后续处理；若未命中，则依据来源轮次、来源节点和证明信息向潜在持有者发起回取请求，获取原始负载并校验其与证明材料的一致性。

第四步，被引用的历史广播结果在当前轮被成功采用，或其生命周期超过预设轮数上限时，对应条目被标记为已消费或过期，防止缓存无限增长。

本机制并没有改变 ACS 的基本决定对象，而是改变了"哪些已可用负载能重新进入后续轮次提案"的候选集合。历史广播结果没有被强行插回当前轮，而是延迟到后续轮次中再次争取被采纳，因此不会直接干扰当前轮 ACS 输出的语义。

== 安全性与活性分析

引用不能替代负载本身。节点在收到引用后，必须通过本地缓存命中或回取路径拿到原始负载，并校验其与来源证明的一致性，才能将该历史结果视为有效输入。即使拜占庭节点声明了一个不存在的引用，诚实节点也不会在没有拿到真实数据的情况下错误地接受它。

本机制改变的是提案的候选集合，而不是 ACS 对输入提案的处理逻辑。历史引用以与新交易相同的方式进入 ACS 输入边界，ACS 核心不需要感知某个提案是否携带了历史引用，因此复用机制可以在不修改 ACS 内核状态机的前提下落地。

本机制只复用"历史上已经满足强可用性条件"的对象，而不把当前轮尚未完成广播的消息提前视为可用。这一顺序保证了后续轮次复用的可恢复性假设不依赖于当前轮的广播进度，从而避免把当前轮尚未成立的可用性条件错误地借用到后续轮次中。

在活性层面，Grace 窗口和回取路径都会带来额外延迟。Grace 窗口设置过大时，节点在每轮结束后的等待时间增加；回取路径若设计不当，缺失数据恢复会拖慢后续轮次推进。这是复用机制在活性层面需要承担的工程代价，也正是 Grace 窗口需要与实际网络条件共同调谐的原因。

== 预期收益与适用范围

本机制的预期收益可以从两个层面理解。第一层是通信收益：相同历史负载不必再次完整广播，只需在后续轮次中携带轻量引用和必要证明，从而降低单位已交付交易对应的重复传播成本。第二层是调度收益：迟到但有效的广播结果从"本轮被浪费的历史成本"变为"下一轮仍可能被采用的资产"，使系统更容易把已经支付过的传播成本转化为真实交付，提升有效吞吐量。

本机制最适合部署在 HoneyBadger/Dumbo 这类仍以 ACS 为核心、且广播输出已携带强可用性证明的实现路径中。对于完全并行广播与共识的 DAG 路线，广播浪费问题在架构层面已经不复存在，跨轮复用对这类协议是冗余的——如总结章所分析的，完全并行广播与共识本身就意味着系统在维护某种偏序关系，这从结构上消除了迟到广播被浪费的可能。对于只具备弱局部可见性而不具备强恢复保证的历史消息缓存，本机制同样不能直接照搬。
