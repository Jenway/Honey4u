= 跨轮次复用机制的设计

== 1. 设计目标

在 Dumbo2 型协议中，每个 epoch 先执行 PRBC 广播，再将收集到的 (n-f) 个完成广播结果送入 MVBA。由于异步环境下消息到达次序无法预测，总会有一部分 PRBC 实例在某些节点上“完成得太晚”，从而错过当前轮进入 MVBA 的时机。若这些结果在语义上已经有效，却只能被当前轮丢弃，那么协议吞吐会被无谓地压低，且先前付出的传播成本无法被后续轮次利用。

跨轮次复用机制的目的，就是把这部分“已完成但未被当前轮吸收”的广播结果保留下来，并在下一 epoch 中以较低代价重新纳入提案过程。这里需要强调的是，复用机制只应改变提案构造方式，不应改变 ACS 的决定语义；它应当是外层的输入增强，而不是对广播阶段或共识阶段内部状态机的改写。

== 2. 可复用对象的定义

不是所有历史消息都可以进入复用池。可复用的最小单位，必须是一个已经完成且能够被任意诚实节点独立验证的广播结果。对 PRBC 而言，这意味着条目不仅包含负载本身，还必须包含与该负载绑定的完成证明。若一个消息只是在本地“见过”，但没有公开可验证的完成证书，那么它不能作为跨轮复用对象；否则，后续轮次中的引用只是在转移信任，而不是在复用已建立的协议事实。

因此，本文将一个可复用条目记为
[
x=(e, i, h, \pi, \ell),
]
其中 (e) 为来源 epoch，(i) 为发送者标识，(h) 为负载哈希，(\pi) 为完成证明，(\ell) 为本地缓存标识。这里 (\ell) 仅用于索引，不承担安全性；真正决定条目有效性的，是 ((e,i,h,\pi)) 的联合绑定。节点在收到引用时，必须能够根据这组信息定位到唯一条目，并验证 (\pi) 与 (h) 的一致性。

这一点的含义很直接：复用的对象不是“某个节点收到过的数据”，而是“已经达到广播完成条件、并且可由外部证明其完成状态的数据”。只有这种对象，才适合在后续轮次中被再次引用。

== 3. 复用池与生命周期

复用机制需要一个有限生命周期，否则历史条目会无限堆积，最终把省下来的通信量重新以缓存和检索成本的形式加回来。为此，节点在每个 epoch 结束后保留一个回收窗口 (G)。在窗口内，节点继续接收迟到但已完成的 PRBC 输出；一旦某条目满足完成条件并且未被当前轮吸收，就进入本地复用池。

复用池中的条目经历四个状态：已缓存、可重提、已消费、已过期。已缓存表示条目已通过完成验证并进入池中；可重提表示条目仍在生命周期内，且尚未被后续 epoch 吸收；已消费表示该条目已经成功进入某一轮的决定结果；已过期表示条目超过存活轮数上限，或者因容量约束被清理。

这个状态机的作用是阻止两类问题。第一类是重复提案：同一历史条目不应在多个后续 epoch 中被无约束地反复引入，否则会制造不必要的带宽膨胀。第二类是陈旧引用：如果条目已经失去上下文意义，继续保留只会增加验证负担。因此，节点在每轮构造提案时，应先根据消费标记和过期条件过滤缓存，再决定是否将其作为复用项提交。

== 4. 提案格式与执行流程

复用机制采用统一的提案格式：
[
\mathsf{proposal} = (\mathsf{new}, \mathsf{refs}),
]
其中 (\mathsf{new}) 表示本轮新生成的交易批次，(\mathsf{refs}) 表示从复用池中选出的历史引用集合。二者共同构成本轮 ACS 的输入。这样做的好处在于，历史条目不是以特殊旁路的形式进入协议，而是与新交易共享同一个输入接口；协议对输入的处理逻辑保持不变，只是输入来源更丰富。

流程可以分为三步。

第一步，在 epoch (e) 结束后，节点于回收窗口 (G) 内继续观察迟到完成的 PRBC 输出，并把满足条件的条目写入复用池。此时条目只获得“可复用”资格，还没有进入下一轮决定集合。

第二步，在 epoch (e+1) 构造提案时，节点从复用池中选取若干尚未过期、尚未消费的条目，将其编码为历史引用并附着到提案中。若本轮同时产生了新交易，则新交易和历史引用可以并列出现。

第三步，其他节点收到提案后，先用引用中的 ((e,i,h)) 在本地缓存中检索；若命中，则直接恢复对应负载并验证证明；若未命中，则根据来源信息向可能持有该条目的节点发起回取。只有当原始负载与完成证明都通过验证后，该历史条目才被视为有效输入。之后，它与普通新交易一样进入 ACS 处理流程。

这一定义有一个重要边界：复用机制只负责把“已完成但尚未被当前轮吸收”的条目重新放回输入空间，而不是把它们插回当前 epoch 的共识状态。前者只是输入增强，后者才会改变协议语义；本文明确只取前者。

== 5. 正确性直觉

安全性上，复用机制不能凭空制造有效性。一个引用本身不是有效输入，它最多只是一个指向已验证条目的索引。若节点无法恢复原始负载，或者完成证明无法通过验证，那么该引用必须被丢弃。也就是说，拜占庭节点无法通过伪造引用把一段不存在的数据推进协议。

更重要的是，复用只使用已经满足完成条件的 PRBC 输出，因此不会破坏 ACS 的基本前提。ACS 关心的是输入集合的可达性和一致决定，而这里的历史条目在被复用之前，已经通过广播阶段的完成证明进入了可验证状态。换言之，复用没有放宽广播阶段的正确性门槛，只是把已经跨过门槛的对象延迟到后续轮次再次使用。

活性方面，回收窗口和回取过程都会带来额外延迟，这是该机制必须付出的代价。窗口过小，会漏掉仍有复用价值的迟到结果；窗口过大，则会拖慢 epoch 切换。回取路径若设计得过于复杂，也会增加缓存命中失败时的恢复成本。因此，参数选择不应脱离具体网络条件，而应根据延迟分布、缓存容量和期望吞吐做折中。

== 6. 适用范围

该机制适用于以 ACS 为核心、且广播阶段能够产生公开可验证完成证明的协议。Dumbo2 正好满足这一前提，因此可以作为本文的主要落点。对于仅提供最终一致投递语义、但没有可复用完成证书的广播原语，不能直接套用这一机制；除非先补足等价的证明结构，否则复用会失去可验证基础。

从协议工程角度看，跨轮复用并不是在“重做一遍广播”，而是在“把已完成的广播结果重新编码为后续轮次可消费的输入”。这一点决定了它应当被放在提案构造层，而不是放在 ACS 内核里。只要这一边界保持清楚，复用机制就可以在不改变原有安全证明骨架的前提下，提高系统对迟到有效输入的吸收率。
