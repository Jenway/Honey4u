#import "sdu-thesis.typ": sdu-thesis, thesis-table

#show: sdu-thesis.with(
  title: "面向异步共识协议的广播数据跨轮次复用机制设计与实现",
  english_title: "Research and Implementation of Cross-Round Broadcast Reuse for ACS-Based Asynchronous BFT",
  author: "",
  school_id: "",
  school: "计算机科学与技术学院",
  major: "计算机科学与技术",
  grade: "",
  supervisor: "",
  date: "2026年5月",

  // 中文摘要一般 300 - 800 个汉字
  abstract_zh: [

  ],
  keywords_zh: ("异步拜占庭容错", "异步公共子集", "HoneyBadger", "Dumbo", "跨轮次复用"),

  // 英文摘要约 200 - 600 个实词
  abstract_en: [
  ],
  keywords_en: ("asynchronous BFT", "ACS", "HoneyBadger", "Dumbo", "cross-round reuse"),

  bibliography_file: "refer.bib",

  acknowledgements: [
    TO BE FILLED
  ],

  appendix: [
    TO BE FILLED
  ],
)

// 题目应简洁、精炼、准确且具概括性，能够准确概括论文（设计）的核心内容，一般不超过 20 字，必要时可增加副标题。

= 绪论
// 约 2000 字

== 研究背景
异步拜占庭容错（Byzantine Fault Tolerance, BFT）共识协议试图在至多 $f$ 个节点任意作恶、网络消息延迟无上界的条件下，仍使诚实节点对同一批交易或区块顺序达成一致。相较依赖同步或部分同步假设的协议，异步 BFT 更适合部署在复杂广域网环境中，因为它不需要通过超时机制来判断对方是否失效，从而能够在网络条件极不稳定时仍保持活性保证。HoneyBadgerBFT 首次给出了工程上可行的异步原子广播实现，并以异步公共子集 ACS 作为核心组件 @miller_honey_2016。

基于 ACS 的异步 BFT 协议通常沿用“广播阶段保证提案可用性，再由一致性阶段选择公共子集”的结构。该结构简洁、模块化程度高，因此在 HoneyBadger 与 Dumbo 系列协议中被广泛采用 @miller_honey_2016; @guo_dumbo_2020。与此同时，它也带来了一个容易被忽视但在工程上相当关键的问题：只要协议已经能从不少于 $N-f$ 个提案中构造出决定结果，剩余若干广播实例即使最终由诚实节点完成，也可能因时序落后而被当前轮直接丢弃。对这些“诚实但迟到”的广播负载而言，网络已经为其支付过带宽成本，而共识层却没有将其利用，最终表现为吞吐量下降和广播冗余增加。

共识协议，即分布式系统中多个节点就某个数据或状态达成一致意见的规则与机制，其中拜占庭容错（Byzantine Fault Tolerance, BFT）共识协议保证了在有节点作恶的情况下仍然保证协议正确。而从时间假设来说，又可以分为：同步、弱同步与异步共识协议，异步共识协议不需要通过超时机制来判断对方是否失效，从而能够在网络条件极不稳定时仍保持活性保证。异步 BFT 共识协议有两种较为主流的实现方式：基于异步公共子集（ Asynchronous Common Subset, ACS） 和基于有向无环图（DAG）。本课题关注的为基于 ACS 的异步 BFT 共识协议。HoneyBadgerBFT 以异步公共子集（Asynchronous Common Subset, ACS）作为核心组件,首次给出了工程上可行的异步原子广播实现 @miller_honey_2016。

== 研究课题：ACS 广播的带宽浪费问题

经典的 ACS 协议可以分为广播和共识两个阶段，且这两部分严格串行执行：广播阶段保证各节点提案的可用性，共识阶段在此基础上选择公共子集。这种 ACS 协议设计结构简洁、模块化程度高，因此在 HoneyBadger 与 Dumbo 系列协议中被广泛采用 @miller_honey_2016; @guo_dumbo_2020。

这种串行结构同时也带来一个问题：由于拜占庭节点可能永不发送数据，协议无法等待所有节点完成广播，而是在收到 $N-f$ 个广播后即进入共识阶段。这意味着，剩余诚实节点的广播结果即便最终完成传播，也可能因时序落后而被当前轮直接丢弃，造成带宽浪费。本文的研究目标是：针对这一问题，在不深入修改 ACS 黑盒的前提下，设计跨轮次复用机制，回收这部分被浪费的广播数据。

本文聚焦于经典 ACS 架构中的广播浪费问题，而不试图改变异步共识的基础安全模型。具体地，设某轮协议中每个节点广播一个提案批次，协议只要求最终输出一个不少于 $N-f$ 个元素的公共子集。当一部分诚实节点的广播在当前轮完成传播但未进入该公共子集时，这些广播结果就形成了可复用但未利用的数据资产。若下一轮仍要求这些节点重新广播语义上等价或高度重叠的交易内容，则系统会重复承担传播成本。任务书已将此现象明确表述为“因网络延迟导致部分诚实节点的广播数据被丢弃从而造成带宽浪费”，并要求在现有开源原型基础上设计优化机制。某些广播负载已经完成传播，却没有进入当前轮的最终决定集合，结果在后续轮次中仍需再次提交和再次传播。跨轮次复用的目的，就是在不破坏一致性与活性的前提下，尽量减少这类重复传播。

== 研究意义

相比依赖同步或部分同步假设的协议，异步 BFT 无需通过超时机制判断节点是否失效，因而在网络条件极不稳定的环境中仍能保持活性，更适合部署于复杂广域网场景。基于 ACS 的异步 BFT 协议相比 DAG 结构的更加容易实现与进行理论验证

基于串行的 ACS 的异步 BFT 共识协议已经有一定的应用，相较于直接迁徙到并行 ACS 结构，本方案不直接大规模修改 ACS 结构，便于已有的系统进行迁徙。

随着分布式系统与区块链技术的发展，如何在存在恶意节点且网络条件恶劣的情况下保障系统一致性，已成为基础设施层面的核心挑战。提升分布式系统的有效吞吐量，是当前区块链与分布式数据库领域的关键需求。本课题提出的广播数据跨轮次复用机制，具有直接的应用参考价值，可转化为系统实际性能的提升。

== 研究目标与主要贡献

本文提出一种面向串行 ACS 协议的跨轮次广播复用方法，通过对诚实但迟到广播数据进行缓存复用来减少带宽消耗。设计了具体的协议消息头，并给出一个具体实现，进行本地仿真测试实验，评估复用机制对吞吐量、延迟和广播冗余的影响，实验结果表明，在代表性的高负载设置下，跨轮复用能够稳定提高端到端吞吐量。

机制设计上，本文提出了一种面向 ACS 协议的跨轮次广播复用方法，复用对象被严格限制为“已经完成广播可用性证明，但未在本轮被采用”的历史输出。

在系统实现上，本文围绕统一提案编码、缓存管理、引用消费和缺失数据回取完成了原型改造。

实验方面，本文评估复用机制对吞吐量、延迟和广播冗余的影响。结果表明，在代表性的高负载设置下，跨轮复用能够稳定提高端到端吞吐量，其中 FIN 风格路径在完整高负载扫点上持续超过任务书要求的 10% 改进门槛。不过，每笔已交付交易归一化后的追踪通信成本并未同步下降，因此本文不把它写成“统一降耗”，而是把它理解为一种能够稳定回收迟到广播成果的外层优化。

在研究方法上，本文采取“协议分析 + 原型实现 + 实验验证”的路线。协议分析部分用于澄清复用对象必须满足的可用性前提，以及机制不会直接修改 ACS 决定语义这一边界；原型实现部分负责把这些前提具体落实到统一提案编码、缓存生命周期管理和缺失数据回取路径之中；实验部分则通过对比复用开启与关闭两种配置，观察吞吐量、追踪通信成本和复用条目数量的变化。

需要说明的是，本文并不试图证明一种适用于所有异步共识架构的统一加速方法。研究对象被明确限制在 HoneyBadger/Dumbo 一类、以 ACS 为核心且广播输出已具备强可用性保证的实现路径中。凡是超出这一边界的协议家族，例如 DAG 风格排序系统、弱可见性消息缓存或尚未稳定实现的候选后端，都只会作为讨论背景，而不会被混入本文的正式贡献表述。



== 论文组织结构

全文按如下结构展开。第二章介绍 ACS、HoneyBadger、Dumbo 及相关优化工作的背景。第三章描述跨轮次复用机制的系统模型、数据结构与正确性边界。第四章给出系统实现方案。第五章设计并分析实验。第六章总结全文并讨论后续工作。

本文的后续章节安排如下：第二章介绍相关工作：梳理现有异步 BFT 协议的发展脉络以及回顾各种优化改进方案。第三章描述跨轮次复用机制的机制设计、系统模型与正确性。第四章给出实验设计与结果分析，介绍系统具体实现与仿真模拟实验所得到的结果并对其分析。第五章总结全文并讨论后续工作。



= 背景与相关工作

// 约 2000 字

== ACS 与异步 BFT

异步 BFT 共识协议在至多 $f$ 个节点任意作恶、网络消息延迟无上界的条件下，仍能确保诚实节点对同一批交易或区块顺序达成一致。 HoneyBadgerBFT @miller_honey_2016 是第一个可用的异步 BFT 共识协议，它指出：可以通过基于更弱的 ACS 来构建异步原子广播（asynchronous atomic broadcast，AAB），进而来实现异步 BFT 共识协议。

ACS 允许 $N$ 个节点各自提出输入值，并最终使所有诚实节点输出完全相同的、包含至少 $N−f$ 个提案的子集（$f$为最大拜占庭节点数）。它的早期理论来源可以追溯到 1994 年 Ben-Or、Kelmer 与 Rabin 对异步安全计算和公共子集问题的研究 @ben-or_asynchronous_1994，这种 ACS 范式被许多文献称之 BKR94 范式；另一个比较主要的范式被称之为 CKPS01，即 2001 年由 Cachin 等人所提出的一种 ACS 结构。

HoneyBadgerBFT 所使用的是 RBC + ABA 的 BKR94 的范式，2020 年的 Dumbo 协议（准确的说是 Dumbo2）对其 ACS 结构作出了改进，引入了 MVBA 来代替 N 个 BA 实例，同时引入 PRBC 来代替 RBC 这一 ACS 结构实际上要更接近于 CKPS01 范式，区别是，正如 HoneyBadger 论文中所提到的： CKPS01 将整个要做共识的内容输入给 MVBA，这导致效率很低，而 Dumbo 的结构通过引入 PRBC，只把证明广播完成后的轻量 Proof 进行共识，提高了效率。

Dumbo2 ACS 结构中的 MVBA 是作为黑盒存在的，因此可以任意替换，一些工作提出了更好的 MVBA 结构，除了 CKPS01，还有例如 Dumbo-MVBA、sDumbo MVBA、FIN MVBA 等，本设计不是对于 MVBA 做出改进，因此之后会将 MVBA 作为黑盒处理。

还有一些工作，如PACE (引入了 RABA)和 FIN （提出了一种 signature-free 的 MVBA协议） 提出了一些更好的 ACS 变体，但大体来说都可以视作 HB/BKR94 以及 Dumbo2/CKPS01 的变体

基于 DAG 的异步 BFT 协议绕开了 ACS，如 DAG-rider，2022 年的 Narwhal and Tusk 将 Mempool 协议和共识协议进行并行解耦，获得了很好的性能，但是在实现上要复杂。


== ACS 与异步 BFT
ACS 的早期理论来源可以追溯到 Ben-Or、Kelmer 与 Rabin 对异步安全计算和公共子集问题的研究 @ben-or_asynchronous_1994。此后，研究者围绕可靠广播、异步二值一致性以及多值一致性不断寻找更高效的组合方式。HoneyBadgerBFT 的重要意义在于，它将这些模块组织为一个工程上可执行的异步原子广播系统，使得 ACS 不再只是理论构件，而成为可部署协议的主干 @miller_honey_2016。

在 HoneyBadger 框架中，每个节点先通过广播模块传播自己的交易批次，再通过并行一致性模块判断哪些广播实例应被纳入公共子集。该结构具有显著的模块化优势，但也意味着广播与决定之间存在天然的“时序错位”：广播一旦完成，其代价就已经发生；而是否进入子集，则取决于后续的一致性推进状态。

从系统视角看，ACS 之所以适合作为异步 BFT 的骨架，在于它把“负载是否广泛可恢复”和“哪些负载应进入当前决定集合”拆成了两个可分别推理的子问题。前者通常由可靠广播或带证明的可用性广播承担，后者则由 ABA、MVBA 或其变体完成。这种模块化设计一方面便于证明安全性，另一方面也使协议实现可以围绕统一的输入输出接口演进。然而，模块化并不等于没有代价。只要广播阶段和决定阶段在时间上不能完全重合，就会存在这样一种可能：某个诚实节点的广播结果在逻辑上已经足够“好”，也就是说它已经满足后续恢复所需的可用性条件，但由于到达或被处理的时机略晚，仍然会被当前轮的决定结果排除在外。本文关注的正是这种结构性浪费，而非更一般的区块打包或交易去重问题。

对于面向工程实现的协议而言，这个问题具有两个额外特点。其一，广播浪费往往不会直接表现为安全性错误，因此它容易在只看吞吐量峰值时被忽略。其二，浪费真正发生的位置通常位于广播模块与上层宿主调度之间，而不完全位于共识核心内部。这意味着，如果只修改 MVBA 轮数或仅优化密码学运算，未必能够直接回收已经被传播却未被采用的那部分历史负载。也正因为如此，本文把研究重点放在“已证明可用的历史广播结果如何跨轮次重用”，而不是再次发明新的 ACS 数学构造。

== 广播与共识进行并行

经典的 ACS 结构分为广播和共识两部分，且这两部分是严格串行的，正如 DispersedLedger 论文中所观察的，这种串行会造成性能问题，因为广播阶段主要消耗带宽资源，而共识阶段主要耗时长。 DL 提出将广播阶段尽可能地缩短，只保证数据能够到达，增加一个和共识阶段并行的 retrival 机制来利用共识阶段的带宽。speeding dumbo 也有类似的机制：在广播阶段不强制保证完全到达，在共识结束后增加一个 retrival 阶段下载可能没到达的数据。

不过，这样的设计其实还是不能完全并行广播和共识，DumboNG 提出了一种更加并行的设计，也就是流水线广播，根据水位线共识。相似的，Narwhal and Tusk 的思路也是将内存池协议和共识协议分开。本文的研究将专注于传统的 ACS 结构，也就是广播和共识阶段严格串行的 ACS 结构。

== 广播带宽浪费问题

广播和共识阶段严格串行会导致一些问题：由于共识阶段依赖于广播阶段，在 BFT 情况下，不可能等待所有节点广播完成（设想有拜占庭节点一直不发包，那么系统就会卡死），而是等待系统中的数据已经广泛可用，具体的来说，比如在 Dumbo 中，当一个节点收到 N - F 个广播消息后，他就会进入共识阶段，那么剩下的 F 个广播消息，即使他们是诚实的，也会被浪费掉。DL 在对于 HoneyBadger 的研究中也提到过类似的情况。只不过由于 HoneyBadger 的 ACS 结构是 N 个 RBC 与 N 个 ABA 实例，即使本地已经有 N - F 个 RBC 完成广播，于是给本地 N - F 个 ABA实例投票，但是另外 F 个实例不一定在全局完成终止，因而在网络较好的情况下其实并不会有很严重的带宽浪费，而对于网络不好的情况，DL 指出 HoneyBadger 也会有类似的问题，并给出了一个 internodelinking 的机制来解决带宽浪费，具体来说，这个机制在消息头维护一个 V 向量与水位线，也就是某种 partial order 信息，让之后的节点能通过这种信息计算出某种消息应该被接受上区块链。但是要指出的是 internodelinking 实质上深入修改了 ACS 的结构，导致这种修改很难迁移到 CKPS01 风格的 ACS 上。

并行的 ACS 协议则不会有这种浪费问题。

== 广播可用性、证明与复用前提

尽管学术文献中常用“广播”一词笼统描述 ACS 前半阶段，但不同协议实现实际依赖的可用性保证并不完全相同。有的路径使用经典 RBC，有的路径使用带纠删码和证明材料的 PRBC，还有的实现会把阈值签名、Merkle 证明或恢复证据作为额外元数据附着在广播输出之上。对本文而言，关键并不在于这些子协议名称是否一致，而在于被缓存并跨轮引用的对象，是否已经携带足够强的可恢复性证据，使其他诚实节点能够在需要时重新获取原始负载。

这一点直接决定了复用机制的适用边界。若缓存池中存放的只是“本地看见过、但无法证明其他节点也能恢复”的临时消息，那么后续轮次中的轻量引用就可能把正确性问题从“是否应当采纳该广播结果”转化为“是否真的有人还能找回它”。这会让复用本身成为新的安全风险。相反，若被引用条目在写入缓存前就已经满足强可用性条件，那么复用阶段只是在时间维度上延长了该条目的使用寿命，而没有改变其可恢复性假设。本文实验中的 Dumbo 路径与 FIN 风格路径，正是建立在这种“先具备可用性，再讨论跨轮复用”的顺序之上。

因此，本文不会把自己的结论表述为“适用于任意 ACS 黑盒”。更准确的说法应是：当广播输出已经具备可验证、可恢复的强可用性保证时，可以在不破坏原有安全边界的前提下，把这些未被当前轮采用的历史结果延迟复用到后续轮次中。这个限制看似保守，实际上恰恰是本文能够在工程实现上成立的前提。

== Dumbo 路线与 MVBA 优化
Dumbo 系列工作尝试减少 HoneyBadger 中并行 ABA 的负担，利用 MVBA 将 ACS 的一致性阶段从“对每个提案单独做二值决定”转化为“围绕一个已验证的候选向量达成一致”，从而显著改善系统的实际吞吐量和延迟 @guo_dumbo_2020。随后，Speeding Dumbo 从广播组件和 MVBA 两个方面进一步压缩具体运行成本，尤其强调广播开销与 MVBA 轮数对整体延迟的影响 @guo_speeding_2022

进一步地，Dumbo-NG 将传播与决定更加彻底地解耦，使系统在高吞吐运行下仍能保持较稳定的延迟，并把“诚实节点广播结果因时序落后而被忽略”上升为协议层面需要解决的审查与效率问题 @gao_dumbo-ng_2022。

值得注意的是，Dumbo 路线给本文的重要启发不只是“MVBA 更快”，而是它揭示了决定阶段候选结构与广播阶段可用性结构之间可以做更精细的接口设计。具体而言，Dumbo 通过把候选向量验证、广播证明和多值一致性过程更紧密地绑定在一起，减少了 HoneyBadger 那种对所有广播实例分别推进 ABA 的代价。本文虽然没有重新设计新的 MVBA，但在“历史广播结果以何种轻量格式进入下一轮提案”这一点上，延续了这种接口思维：不是重新发送完整负载，而是发送带有来源与证明绑定信息的引用。

这也是为什么本文更适合放在 Dumbo 及其后继工作所代表的工程优化脉络中理解。它并不试图替代 Dumbo 的 MVBA，也不试图与 Dumbo-NG 的整体架构重构竞争，而是在已有 ACS 路线内部，进一步回收本来会被宿主层浪费掉的可用广播结果。

== 其他相关方向
在 ACS 框架内部，PACE 和 FIN 分别代表了两种不同的性能优化路线：前者通过重新设计可重提的一致性框架，改善传统 BKR 结构的并行瓶颈 @zhang_pace_2022；后者则在信息论与无签名设置下实现了常数时间 ACS，并展示了异步公共子集仍有明显的结构优化空间 @duan_fin_2023。与此同时，Narwhal/Tusk 和 DispersedLedger 一类 DAG 或分离式架构则通过把传播和排序进一步解耦，从根本上绕开经典 ACS 中的某些广播浪费问题 @danezis_narwhal_2022; @yang_dispersedledger_2022。本文的关注点并不在 DAG 协议，而在于回答一个更受任务书约束的问题：如果仍坚持 ACS 作为黑盒，能否通过跨轮缓存与引用机制有效回收迟到广播结果。

从研究方法上看，PACE、FIN 与 DAG 路线共同说明了一件事：异步 BFT 仍然存在大量值得挖掘的结构空间。但这些工作大多聚焦于协议核心的重新设计，例如改变一致性推进方式、改变广播与排序的耦合模式，或者改变系统对密码学原语与消息复杂度的整体取舍。相比之下，本文采取的是一个更工程化、也更贴近任务书的问题切口：在不推翻现有 HoneyBadger/Dumbo 风格 ACS 黑盒的前提下，回收那些已经完成可用性传播但被当前轮时序忽略的结果。换句话说，本文不与这些工作竞争“谁定义了更快的异步共识协议”，而是回答“对已有协议原型进行最小侵入改造，是否能够显著减少带宽浪费”。

== 本文的研究定位
综合上述工作，可以把本文放在三条边界的交叉点上理解。第一，本文属于 ACS 路线，而不属于 DAG 路线，因此它默认接受“广播与子集决定是两个相对独立阶段”这一系统结构。第二，本文是宿主层与实现层优化，而不是新的基础协议证明工作，因此它强调缓存、引用、生命周期、驱动接口与实验复现，而不是重新给出全新的多值一致性复杂度上界。第三，本文依赖被复用对象已有强可用性保证，因此它比“适用于任意历史消息重放”的泛化叙述更保守，但也更容易与现有工程实现对齐。

这种定位恰好符合本科毕业设计的约束。一方面，它避免把课题扩展成过于庞杂的协议家族比较；另一方面，它又保留了足够清晰的技术贡献，即：通过统一提案编码、可复用广播池和缺失数据回取路径，把诚实但迟到的广播输出从“被浪费的历史成本”转化为“可延迟消费的协议资产”。



= 跨轮次复用机制的设计

// 约 3000 字

== 问题建模与系统假设

考虑如下系统模型。

- 设系统中有 $n$ 个节点 ${P_i}_{i in [n]}$，其中 $[n]$ 表示整数集 ${1, 2, limits(dots) , n}$。这些节点的地址信息保证公开可信，例如通过公钥认证体现保证。
- 系统中最多存在 $f$ 个静态的故障的（faulty）节点（满足 $N gt.eq 3f + 1$）。攻击者在协议开始前可选择并完全控制任意 $f$ 个节点，获得这些节点的初始内部状态，并能在协议执行期间使这些节点任意地发生异常行为。
- 网络为异步认证点对点网络。任意两个节点之间都存在认证信道；攻击者可以任意延迟、重排或丢弃消息。但诚实节点之间发送的消息（若无限等待）最终都会被送达。

在该模型下，我们希望构造一个 $(n)$ 节点间的原子广播（atomic broadcast）协议。该协议按连续的 epoch 执行；在每个 epoch 结束后，系统输出一批新的交易并将其追加到已提交日志中。原子广播协议需以压倒性概率满足以下性质：

- 一致性（Agreement）。如果某个诚实节点输出一个值 $(v)$，那么所有诚实节点都会输出 $(v)$；
- 全序性（Total order）。如果两个诚实节点分别输出值序列 $⟨(v_0, v_1, limits(dots), v_j)⟩$ 和 $⟨(v'_0, v'*1, limits(dots), v'*{j'})⟩$，那么对于所有 $(i \le \min(j, j'))$，都有 $(v_i = v'_i)$
- 抗审查性（Censorship resilience）。如果某个值 $(v)$ 被 $(n - f)$ 个诚实节点作为输入提交，那么最终每个诚实节点都会输出该值。

我们采用与 HoneyBadgerBFT 类似的构造思路，即先实现异步公共子集（asynchronous common subset, ACS），再结合门限加密将 ACS 转化为原子广播。

存在一个有效的 ACS 协议，满足以下性质：

- 一致性（Agreement）。若某个诚实节点输出集合 $(V)$，则所有诚实节点都输出 $(V)$；
- 有效性（Validity）。若某个诚实节点输出集合 $(V)$，则 $(|V| gt.eq n - f)$，且 $(V)$ 至少包含 $(n - 2f)$ 个诚实节点的输入；
- 完备性（Totality）。如果有 $(n - f)$ 个诚实节点提供了输入，则所有诚实节点最终都能输出结果。

除此之外，本文进一步要求所采用的 ACS 协议必须保证：

- 满足严格的两阶段结构：广播阶段与共识阶段必须串行执行，不能并行交叠。
- 广播阶段中的每个实例都必须携带可验证的完成证明，使得节点能够判断某个广播实例是否已经达到足够可用的状态并可安全地进入后续共识阶段。

Dumbo2 协议完整满足我们的需求，我们接下来就直接以 Dumbo2 协议为例，其详细算法我们将摘抄到附录中。

Dumbo2 的 ACS 子协议由两部分组成：PRBC 广播阶段与 MVBA 共识阶段。其基本流程是：每个节点首先并行执行各自的 PRBC 实例；当某个节点收集到至少 $(n-f)$ 个 PRBC 的 finish message 及其相应证明后，便将对应向量 $(W)$ 作为 MVBA 的输入。由于进入 MVBA 的输入数量固定为 $(n-f)$，其余最多 $(f)$ 个 PRBC 实例即使在局部视角下稍后完成，也不会再进入当前 epoch 的共识输入集合。

我们希望构造一种基于 Dumbo2 的改进协议：它在保持 ACS 与原子广播安全性的同时，能够将上一 epoch 中未及时进入 MVBA 的有效广播结果缓存起来，并在下一 epoch 中以更低开销重新注入提案流程，从而提高系统吞吐与端到端可见性。

== 复用前提与复用对象的界定

并非所有历史广播结果都能安全地进行跨轮次复用。以经典 RBC 为例，其只保证最终一致投递，但并不天然提供一个可供任意诚实节点独立验证的公开完成证明。若恶意节点伪造一个轻量级引用，而该引用所指向的广播结果实际上并未达到协议所要求的可用性状态，那么后续轮次中的复用过程就可能把错误信息重新注入协议，从而破坏整体正确性。

因此，我们要求 ACS 的广播子协议必须满足预定义的完成条件，并且在完成时能够产出可验证的完成证明，使任意诚实节点都能据此独立判断该广播结果是否已经可复用。

满足这一要求的典型子协议是 PRBC。PRBC 在广播完成时会产生公开可验证的证据，例如阈值签名或等价的完成证书，使节点无需重新执行广播过程即可确认该条目已经达到协议所要求的可用性状态。跨轮次复用机制正是建立在这种公开可验证性之上。

对于仅具备局部可见性、但无法生成可验证完成证书的广播路径，本文的跨轮次复用机制不能直接适用。不过，在某些协议中，可以通过将原有 RBC 替换为 PRBC，使其满足复用所需的前提条件。例如，原版 HoneyBadger 协议的广播阶段采用 RBC，不满足本文的要求；若将其替换为 PRBC，则可以在保持广播语义兼容的前提下引入跨轮次复用机制。由于 PRBC 完全满足 RBC 所要求的正确性性质，这种替换在安全性上是可接受的。需要指出的是，PRBC 通常比 RBC 更昂贵，因此该替换可能会对协议的整体性能带来一定影响。

== 核心数据结构与提案编码

为了使历史广播结果能够被后续轮次安全引用，每个可复用条目必须具有稳定且可验证的身份。我们将一个条目记为

$
  upright("item") = (e, i, h, pi, upright("id"))
$

其中 $e$ 表示来源轮次，$i$ 表示广播发送者，$h$ 为广播负载的根哈希或摘要，$pi$ 为对应完成证明，$upright("id")$ 为用于缓存索引的轻量标识。

其中，$upright("id")$ 仅用于本地命中与跨轮索引，不承担安全性。真正约束引用正确性的，是 $(e, i, h, \pi)$ 之间的绑定关系。节点在接收到某个引用时，必须能够根据这些信息唯一定位到目标条目，并通过验证证明 $pi$ 确认该条目确已满足广播完成条件。

在提案格式上，我们采用“内联新负载 + 历史引用”的统一表示。也就是说，一个 ACS 输入既可以包含本轮新生成的交易批次，也可以包含若干历史条目的引用，二者共同构成本轮提案内容。

这种设计有两个工程优势：一是无需重新发送历史数据的完整负载，显著降低通信开销；二是复用机制被显式纳入协议输入边界，协议层与运行时可以共享同一线格式，无需为复用项设计独立的共识路径。其正确性因此可以直接在 ACS 的输入/输出语义之上讨论。

== 生命周期管理与重提策略

复用机制的关键在于明确规定历史数据的写入、重提与删除条件。为此，本文为每个可复用条目引入有限生命周期。

一轮 ACS 结束后，节点并不立即丢弃所有未被当前轮共识吸收的广播结果，而是在一个有限的回收窗口（Grace Window）内继续观察仍可能到达的、且携带完整证明的迟到输出。只有在该窗口内完成且满足可用性要求的条目，才会被写入本地复用池。

除了单个条目的字段外，复用池还隐含维护了条目的状态转换。每个缓存条目在其生命周期内经历四个状态：

1. 已缓存：条目已满足保留条件并进入复用池；
2. 可重提：尚未在后续轮次中被成功纳入决定结果，且仍处于生命周期窗口内；
3. 已消费：该条目已经通过某一轮提案被正式采用；
4. 已过期：存活轮数超过上限或因缓存容量约束被清理。

在下一轮构造提案时，节点并不会无上限地重提所有历史条目，而是依据预设的 pool_reuse_limit_per_round 参数进行裁剪，以平衡“尽量多回收迟到结果”与“避免下一轮提案被历史引用淹没”之间的矛盾。同时，结合消费标记执行去重检查，排除已经被采用的缓存项，防止复用退化为无意义的重复引用。

重提策略还有一个容易被忽视的约束：被再次引用的条目必须尚未在后续轮次中成功消费。否则，复用就会退化成无意义的重复引用，既增加元数据开销，也可能干扰宿主层对“本轮真正新增贡献”的判断。因此，消费标记和去重策略并不是实现细节，而是复用机制本身正确性的组成部分。

复用机制的关键不在于简单地保存历史数据，而在于明确规定其写入、重提与删除条件。为此，我们为每个可复用条目引入有限生命周期。

一轮 ACS 结束后，节点并不立即丢弃所有未被当前轮共识吸收的广播结果，而是在一个有限的回收窗口 (G) 内继续观察仍可能到达的、且携带完整证明的迟到输出。只有在该窗口内完成且满足前述可用性要求的条目，才会被写入本地复用池。窗口 (G) 的长度是一个协议参数：过短会丢失本可复用的迟到结果，过长则会增加轮次切换时的等待成本，削弱吞吐收益。

每个缓存条目在其生命周期内经历四个状态：已缓存、可重提、已消费和已过期。已缓存表示条目已满足保留条件并进入复用池；可重提表示它尚未在后续轮次中被成功纳入决定结果，并且仍处于生命周期窗口内；已消费表示该条目已经通过某一轮提案被正式采用；已过期则表示其存活轮数超过上限或因缓存容量约束被清理。为避免同一历史条目被反复引用，节点在每轮构造提案时必须执行去重检查，并结合消费标记排除已经被采用或已失效的缓存项。

== 机制执行流程

跨轮复用机制的执行可以概括为以下三个阶段。

首先，在某轮 ACS 决定结束后，节点于回收窗口 (G) 内继续收集迟到到达但已满足完成条件的广播结果，并将其连同证明材料写入复用池。此时条目仅获得“可复用”资格，并不意味着它已经进入下一轮的决定集合。

其次，在下一轮准备提案时，节点从复用池中筛选尚未过期且尚未被消费的条目，将其编码为历史引用并并入新的提案。若该节点同时存在本轮新产生的交易，也可以一并作为内联负载发送。由此，历史结果与新结果在协议层面处于同一输入框架下，而不需要为复用项设计独立的共识路径。

最后，其他节点在收到带引用的提案后，先依据 (\mathsf{id}) 进行本地缓存查询；若命中，则直接恢复对应负载并验证其证明一致性；若未命中，则根据 ((\mathsf{epoch}, \mathsf{sender}, h)) 向潜在持有者发起回取请求，获得原始负载后再完成校验。只有在原始负载与完成证明均通过验证后，该历史条目才可作为有效输入参与后续 ACS 处理。若该条目在规定轮数内仍未被采用，则将其标记为已过期并从缓存中删除。

需要强调的是，复用机制并不改变 ACS 的决定对象，也不改变当前轮共识的语义。历史条目并未被“插回”正在进行的轮次，而是作为下一轮输入的一部分重新争取进入决定集合。因此，复用机制的作用范围仅限于输入构造与负载恢复，不影响 ACS 核心的安全性定义。

复用机制的执行流程可概括为四步。

第一步，在某一轮 ACS 决定结束后，节点并不立刻丢弃所有未入选广播结果，而是在一个有限的等待窗口内继续观察迟到但有效的广播输出，并把它们写入本地复用池。

第二步，在下一轮准备提案时，节点从复用池中选择若干尚未过期且尚未被消费的条目，把它们编码为引用集合并附着到新的 ACS 提案中。若本轮自身仍有新交易，也可以一起作为内联负载发送。

第三步，当其他节点收到带引用的提案后，会先查本地缓存。如果命中，就直接用引用对应的历史负载参与后续处理；如果未命中，则根据引用中携带的来源信息和广播证明发起回取，向潜在持有者请求原始负载。

第四步，当被引用的历史广播结果最终在当前轮被成功采用，或其生命周期超过预设轮数上限时，对应条目会被标记为已消费或过期，从而避免缓存无限增长。

把这四步连在一起看，可以发现本机制并没有改变 ACS 的基本决定对象，而是改变了“哪些已可用负载能重新进入后续轮提案”的候选集合。也就是说，当前轮没来得及进入决定集合的历史广播结果，并没有被强行插回当前轮，而是被延迟到后续轮次中再次争取被采纳。这种处理方式避免了对当前轮 ACS 输出语义的直接修改，因此更容易与现有宿主和测试框架兼容。

== 安全性与活性分析

复用机制引入后，必须保证不破坏原 ACS 协议的安全边界。

在安全性方面，该机制的首要约束是：引用不能替代负载本身。节点即使收到了某个历史引用，也必须通过本地命中或回取方式获得原始负载，并完成证明验证，才能将其视为有效输入。换言之，拜占庭节点仅声明一个引用并不足以使诚实节点接受对应条目；若无法恢复原始数据或证明校验失败，该引用应被视为无效。其次，复用机制仅改变提案候选集合，而不干涉 ACS 内部处理逻辑。复用仅针对已经满足强可用性条件的对象，不对当前轮尚未完成的广播结果作提前假设，从而维持了协议证明中对数据可恢复性的严格要求。

在活性方面，复用机制会引入一定的额外时延开销。Grace 窗口过大会拖慢轮次切换速度；缺失数据回取路径若设计不当，可能阻碍后续轮次推进。因此，复用机制并不消除迟到成本，而是试图通过少量等待时间与元数据开销的交换，将已经发生的传播开销重新转化为可被后续轮次消费的有效输入。

该机制的首要安全约束是：引用不能替代负载本身。节点即使收到了某个历史引用，也必须通过本地命中或回取方式获得原始负载，并完成证明验证，才能将其视为有效输入。换言之，拜占庭节点仅声明一个引用并不足以使诚实节点接受对应条目；若无法恢复原始数据或证明校验失败，该引用应被视为无效。

第二，复用机制改变的是提案候选集合，而不是 ACS 的内部处理逻辑。历史引用以与新交易相同的方式进入输入边界，ACS 核心无需区分某一输入是新产生的还是复用而来的。这一点保证了复用机制可以作为外层增强模块叠加在现有 ACS 之上，而不必重写其共识主体。

第三，复用仅针对已经满足强可用性条件的对象，不对当前轮尚未完成的广播结果作提前假设。这样可以避免把尚未成立的完成性结论不当地延伸到后续轮次，从而维持协议证明中对可恢复性的严格要求。

在活性方面，回收窗口与回取流程都会引入额外延迟。窗口过大时，轮次切换会变慢；回取路径若设计不当，则可能阻碍后续轮次推进。因此，复用机制的参数需要结合具体网络延迟分布进行选择。它并不消除迟到成本，而是试图将已经发生的传播开销重新转化为可被后续轮次消费的有效输入。


从安全性角度看，复用机制需要同时回答两个问题。第一，引用是否会让节点在没有真正拿到原始数据的情况下错误地接受某条历史广播结果。第二，引用是否会把原本只在当前轮成立的可用性假设错误地“借用”到后续轮次中。对第一个问题，本文的回答是：引用不能直接替代负载本身，它只能作为查找和验证入口。若节点本地没有命中缓存，就必须通过回取路径重新拿到原始负载并校验其与来源证明的一致性。对第二个问题，本文的回答是：只有已经满足强可用性条件的广播输出才允许进入缓存，因此后续轮次复用的是“历史上已经可恢复的对象”，而不是把当前轮尚未完成的消息提前视为可恢复。

从活性角度看，复用机制也会带来额外代价，最明显的是 Grace 窗口和回取路径本身都会消耗时间。如果窗口设置过大，节点会把更多时间花在等待潜在迟到输出上；如果回取路径设计不当，缺失数据恢复又可能拖慢后续轮次推进。这也是为什么本文实验中专门考察 `pool_grace_ms` 的敏感性，并在网络扰动预备实验中观察慢诚实节点场景下的收益变化。换言之，复用机制不是“免费吞吐提升”，而是通过少量等待与元数据开销，换取更高的历史广播结果利用率。


== 预期收益与适用范围

跨轮复用机制的收益主要体现在两点。其一是通信收益：历史负载不必在每一轮中完整重传，只需携带轻量引用及必要证明，从而减少重复传播开销。其二是调度收益：原本在当前轮中因时序原因未能进入共识的有效广播结果，可以在后续轮次中继续争取被采用，从而提高已发生传播成本的利用率。

该机制尤其适合于以 ACS 为核心、且广播阶段能够产生公开可验证完成证明的协议族（例如 Dumbo2 方案）。对于完全并行、以 DAG 方式组织输入传播的协议，其结构已经将广播与共识的复用关系内生化，迟到广播被浪费的问题并不以相同形式存在，因此本文设计的跨轮复用机制对其并非必要。

跨轮复用机制的收益主要体现在两点。其一是通信收益：历史负载不必在每一轮中完整重传，只需携带轻量引用及必要证明，从而减少重复传播开销。其二是调度收益：原本在当前轮中因时序原因未能进入共识的有效广播结果，可以在后续轮次中继续争取被采用，从而提高已发生传播成本的利用率，并改善系统对迟到但有效输入的吸收能力。

该机制尤其适合于以 ACS 为核心、且广播阶段能够产生公开可验证完成证明的协议族，例如 Dumbo2 一类方案。对于完全并行、以 DAG 方式组织输入传播的协议，其结构已经将广播与共识的复用关系内生化，迟到广播被浪费的问题并不以相同形式存在，因此跨轮复用并非必要。对缺乏强可恢复证明的广播原语，本文机制亦不宜直接套用，除非首先补足等价的可验证完成条件。

综合前述设计，可以把本机制的预期收益理解为三层。第一层是最直接的通信收益：相同历史负载不必再次完整广播，只需在后续轮次中携带轻量引用和必要证明。第二层是宿主调度收益：迟到但有效的广播结果从“本轮被浪费的历史成本”变为“下一轮仍可能被采用的资产”，因此系统更容易把已经支付过的传播成本转化为真实交付。第三层是实验收益：由于驱动层可以同时观察吞吐量、引用数量和追踪通信成本，复用是否真的有效不再只依赖单一 TPS 指标判断。

本机制依赖被跨轮引用的数据在被缓存时，已经由广播子协议提供足够强的可用性保障。例如 Dumbo 中由 PRBC 输出及其阈值证明提供；纯 RBC 风格的协议不能直接套用。

= 实验设计与结果分析

// 实验过程应当近似 2000 字

== 系统架构与实现

我们实现了这样一个系统：包含节点运行时，传输层与 ACS 黑盒。简单的来说，节点运行时会在每个轮次里不断的生成交易，将交易放到交易池，并将交易id输入到 ACS 黑盒中，ACS 输出事件（Event），包括单播消息（send）、广播消息（broadcast）、某个广播消息已经广泛可用（ProposalAvailable）、输出决定子集（Decided）以及可能的失败事件。注意到，我们对于 ACS 输入的并不是原始交易输入，这里的考量是，当我们引入复用机制时，复用的轻量级索引和交易其实并不是一种数据结构，让 ACS 来处理这种复杂度会增加系统的耦合度，相反的，让 ACS 并不持有交易数据，而是只持有 id，这样无论是普通交易还是索引对于 ACS 还是透明的，像前述的递归查询索引对应数据以及拉取交易都可以交给运行时来处理。设置一个 grace time 超参数，其允许 ACS 在结束之后还能继续运行给定时间，runtime 从而将迟到广播数据缓存到内存池中。

原型系统由三层组成。协议层负责 ACS、PRBC 与复用相关逻辑；密码学与编码层负责哈希、证书、消息序列化以及相关的阈值签名接口；驱动层负责多进程运行、网络注入、参数配置与结果汇总。实验使用本地多进程 TCP 环境，以避免单进程事件循环对时序的系统性扭曲。对每轮执行过程的统计包括：端到端吞吐、ACS 阶段吞吐、单位已交付交易的追踪通信成本，以及复用条目的使用情况。

复用池的实现遵循前述协议定义：在一轮决定结束后，节点在有限的 Grace 窗口内继续接收迟到但已完成的广播结果；满足完成条件的条目进入本地缓存，并在后续轮次以轻量引用的形式重新参与提案。为避免缓存无限增长，系统对每轮可重提条目数、条目存活轮数和缓存总容量均设置上界。若引用在本地未命中，则通过回取路径恢复原始负载并验证其完成证明；若验证失败，则该引用被丢弃。

我们基于 rust 实现了这个复用机制，对于 ACS 黑盒，我们复用了 Dumbo 的 python 实现，为了证明复用机制通用性，我们还用 rust 实现了修改版的 FIN 协议 ACS（我们将其中广播阶段的 RBC 替换为了 PRBC，正如我们上一节所论证的，这个替换并不会损害 ACS 协议的正确性）

本文实现基于一个 HoneyBadger/Dumbo 风格的异步 BFT 原型，将跨轮次复用放在 ACS 输入边界和外层宿主调度之间，而不是改写 ACS 内部的一致性语义。系统整体可以分为三层：底层密码学和消息编码层负责 Merkle 证明、阈值签名、阈值加密和线格式转换；协议层负责 HoneyBadger、Dumbo 及 FIN 风格 ACS 的推进；运行时层负责节点进程、网络收发、提案构造、复用池维护和实验指标采样。

这种分层的目的，是让复用机制既能接触到广播输出和下一轮提案，又不侵入 ACS 的决定逻辑。对 ACS 而言，输入仍然是一个待广播的 proposal payload；对运行时而言，这个 payload 可以由当前轮新交易和历史广播引用共同构成。这样，复用机制的主要工作集中在“怎样表示历史结果”“何时把历史结果再次提案”“缺失负载如何恢复”三个问题上。

== 统一提案编码与复用池实现

跨轮次复用机制首先落在提案表示上。若仍沿用“每轮只携带完整新负载”的单一提案格式，那么历史广播结果即使已经被缓存，也没有统一入口重新回到 ACS 输入中。为此，本文实现了“内联负载 + 引用集合”的兼容编码：提案既可以直接携带当前轮新交易，也可以携带若干来自历史广播结果的轻量引用。每个引用条目绑定来源轮次、来源节点、条目标识、根哈希及其证明材料，从而使上层驱动能够区分“新广播负载”和“已证明可用的历史结果”。

与单纯做一个缓存字典相比，这种编码改造有两个工程价值。其一，复用机制不再依赖某个特定宿主的内部状态，而是通过明确定义的线格式进入 ACS 输入边界。其二，统一编码可以被不同运行路径共同消费，避免在实现边界上维护两套互不兼容的提案语义。换言之，复用机制真正被实现为协议输入的一部分，而不是测试脚本额外拼接出来的旁路逻辑。

复用池本身负责管理这些历史条目的生命周期。一轮决定结束后，节点保留一个有限的观察窗口，用于截留迟到但有效的广播输出；随后根据每轮重提上限、缓存有效轮数和整体容量上限控制可复用条目的规模。这样做的核心目的是让“回收迟到结果”与“避免缓存无限膨胀”同时成立。若没有这些边界，复用池很容易从优化手段演化成新的内存压力来源。

== 缺失数据回取与宿主边界

仅有历史引用还不够，因为其他节点在收到带引用的提案时，未必本地已经缓存了对应负载。因此，系统还需要缺失数据回取路径。其基本过程是：节点先根据条目标识查找本地复用池；若命中，则直接把引用还原为历史负载；若未命中，则依据来源轮次、来源节点和证明信息向潜在持有者发起回取请求。本文把回取路径作为机制完整性的一部分，但实验主结论仍主要来自缓存命中条件下的复用收益。

从软件架构上看，回取路径也是区分“ACS 黑盒”和“外层宿主”的关键。ACS 本身只看到统一编码后的提案输入，而缺失数据查找、引用展开、缓存命中与网络回取主要发生在宿主层与驱动层边界。这个设计使复用机制可以在不大幅改写 ACS 内核状态机的前提下落地，同时也解释了为什么本文大量实现工作集中在运行时、事件接口和编码边界，而不是单纯改写某个子协议证明。

== 实验环境与方法

除了协议代码本身，本文还实现了面向对照实验的运行支撑。实验配置统一描述节点规模、轮数、批处理大小、复用参数、宿主后端及网络扰动参数；运行脚本按配置展开实验矩阵，启动节点进程并汇总吞吐量、延迟、复用条目和追踪通信成本等指标。这样做的意义不只是减少人工操作，还在于保证同一组参数下“复用关闭”和“复用开启”之间的差异能够被直接比较。

运行时还提供了受控扰动入口，包括慢诚实节点、固定发送延迟、随机抖动以及两类最小边界故障。本文正文只选取与“诚实但迟到”问题最贴近、且结果较完整的场景进行分析。更偏压力测试性质的扰动结果不作为主结论，而是用于说明系统已经具备继续扩展实验的基础。

总体来看，本文实现已经覆盖跨轮次复用所需的关键路径：历史广播结果能够被编码为带证明绑定的引用，复用池能够在有限生命周期内管理这些引用，后续轮次能够把引用与新负载一起提交给 ACS，缺失数据可以通过回取路径补齐，运行时则能够记录吞吐量、延迟、复用条目和追踪通信成本。

实验部分围绕两个核心问题展开。第一，跨轮复用能否显著降低重复广播并提升有效吞吐量。第二，这种提升是否同时体现为单位已交付交易通信成本的下降，而不是仅仅依赖更激进的数据堆积。为此，本文将指标分为四类：端到端吞吐量、阶段时延、驱动层可追踪通信字节数，以及复用条目使用情况。考虑到复用开启后总交付交易数会增长，本文在通信分析中优先采用“每笔已交付交易对应的追踪字节数”这一归一化指标，而不直接以总字节数作为唯一结论依据。

我们在本地网络中通过多进程模拟真实网络环境，我们使用 QUIC 作为通信协议，并通过特殊设计的运行时来模拟诸如网络干扰以及作恶节点的情况。

本文正文采用本地多进程 QUIC 实验结果作为主证据。所有节点均以独立 OS 进程运行，并经回环地址进行真实收发。之所以优先采用 QUIC 结果，是因为这组实验完整覆盖高负载主表、Grace 微实验以及慢诚实和最小边界故障场景，能够较集中地回答本文关心的复用收益问题。

为了避免只在单进程模拟中验证机制，本文实验采用多进程本地运行方式，每个逻辑节点都拥有独立进程并通过本地网络收发消息。运行时驱动负责节点生命周期、网络收发、宿主调度、统计采样和结果汇总。由于驱动层能够记录每轮发送次数、提案字节数、证书字节数、复用条目数量以及队列峰值等指标，它可以比纯协议日志更稳定地刻画“追踪通信成本”。

在宿主实现上，本文比较了 Dumbo 路径和 FIN 风格路径。正文主要使用这两条路径，是因为它们都能完成复用开启与关闭的直接对照，适合支撑本文的机制性结论。两者的绝对吞吐量并不直接代表协议优劣，后文分析主要关注同一路径内复用开启相对关闭的变化。

为避免把单一传输实现的偶然波动误写成结论，本文还使用同结构的 TCP 实验对若干代表点做了交叉核验。TCP 结果没有被用作正文主表，一方面因为 QUIC 扫点更完整，另一方面是因为论文核心问题是“跨轮复用是否稳定回收迟到广播结果”，而不是比较传输栈本身优劣。交叉核验的作用仅限于确认方向一致：在 TCP 代表点上，复用开启相对关闭同样保持正向 `TPS_wall` 提升。

在配置上，本文统一固定 $N=12, f=3$。高负载主结果采用批处理规模 $\{8, 12, 16, 24, 32, 48, 64\}$，每组运行 8 轮并重复 2 次；比较对象包括 Dumbo ACS 路径与 FIN 风格 ACS 路径，两者都分别执行“复用关闭”和“复用开启”两组测试。Grace 微实验固定批处理规模 32、轮数 4，对 `pool_grace_ms \in \{50, 100, 200, 400\}` 各重复 2 次。边界场景包括慢诚实节点、静默节点和损坏回取响应三类。除特定实验另有说明外，复用参数统一固定为 `pool_reuse_limit_per_round = 4`、`pool_expire_rounds = 10`、`pool_mempool_max = 1024` 与 `enable_pool_fetch_fallback = true`。

除主结果外，实验还包含受控网络扰动配置，例如固定发送延迟、随机抖动以及慢诚实节点的额外发送延迟。这使“诚实但迟到”的问题不只停留在理论讨论中，也可以在受控运行条件下观察其对复用收益的影响。

== 结果口径与统计说明

为了避免实验表述含混，本文对几项常用指标的口径做统一约定。`TPS_wall` 指按每轮墙钟时间累计得到的端到端吞吐量，更接近用户视角的实际交付效率；`TPS_acs` 则只统计 ACS 驱动阶段时间，更适合分析协议内部推进开销。`tracked_driver_bytes_total` 及其归一化形式 `bytes/tx` 不是底层网卡抓包得到的物理总字节数，而是驱动层组合统计出的字节表面量。本文实验中，它等于 `send_payload_bytes_total + proposal_available_payload_bytes_total + proposal_available_proof_bytes_total`：前一项反映驱动向 ACS 外层发送的负载字节，后两项反映宿主已经解析出的 proposal 负载与可用性证明材料大小。因此，这个指标更适合被理解为“驱动层可观察到的协议相关字节表面”，而不是严格意义上的链路通信字节。为避免误导，本文仍沿用较保守的中文称呼“追踪通信成本”，但后文对它的解释将始终限制在同一实现口径下的相对比较。复用条目数则指在实验窗口内被成功纳入提案并实际被统计到的历史条目数量，用于说明收益是否真的来自跨轮复用，而不是仅由偶然的调度波动造成。

当比较复用开启与关闭两组结果时，本文优先关注三个问题：第一，吞吐量是否提升；第二，单位交付交易对应的追踪通信成本是否下降；第三，复用条目数是否达到与收益量级相匹配的规模。若只有第一项改善而后两项没有相应支撑，则说明收益更接近“吞吐恢复”而非“通信效率同步改善”；若第二项改善而第一项没有提升，则说明优化可能只是节省了字节但没有转化成更高交付效率。本文结果明显属于前一类情形，因此正文讨论将把“稳定吞吐提升”和“追踪通信成本未同步下降”并列呈现，而不再沿用更乐观的统一降耗叙述。

== 高负载主结果
表 \#ref(tbl-highload-pilot) 给出了 QUIC 高负载实验中的代表性结果。为了避免单纯比较总字节数所带来的误导，表中同时给出吞吐量变化和每笔已交付交易对应的追踪通信字节变化。这里选择 $b \in \{16, 32, 48\}$ 作为正文代表点，是因为它们分别对应中等负载、典型负载和较高负载区间；完整扫点仍覆盖 $\{8, 12, 16, 24, 32, 48, 64\}$。

#thesis-table(
  columns: (1.2fr, 0.8fr, 1.2fr, 1.2fr, 1.1fr),
  header: ([后端], [批处理], [`TPS_wall` 提升], [`bytes/tx` 变化], [平均复用条目]),
  rows: (
    [Dumbo],
    [16],
    [+9.79%],
    [+11.26%],
    [21.0],
    [Dumbo],
    [32],
    [+10.13%],
    [+7.22%],
    [21.0],
    [Dumbo],
    [48],
    [+15.56%],
    [+2.04%],
    [21.0],
    [FIN-style],
    [16],
    [+21.90%],
    [+21.68%],
    [21.0],
    [FIN-style],
    [32],
    [+18.93%],
    [+13.91%],
    [21.0],
    [FIN-style],
    [48],
    [+19.12%],
    [+8.27%],
    [21.0],
  ),
  caption: "N=12, f=3, QUIC 高负载实验中复用开启相对基线的代表性变化幅度",
) <tbl-highload-pilot>

从表 \#ref(tbl-highload-pilot) 可以看出，复用开启后两条路径都保持了稳定的正向收益，而且这种收益并不局限于个别批处理点。完整 QUIC 扫点中，Dumbo 路径在全部七个批处理规模上的 `TPS_wall` 提升约为 7.80% 到 15.56%，FIN 风格路径则维持在约 16.54% 到 21.90% 之间。就相对增幅而言，FIN 风格路径更稳定，也更高一些，这说明即使宿主层开销已经相对收紧，跨轮复用仍然能够回收一部分迟到广播输出。另一方面，Dumbo 路径的绝对吞吐量依然更高，因此这里不宜简单把结果解释为“某个后端整体更优”，更合理的理解是：复用机制在两类实现上都有效，只是收益表现方式不同。

还需要指出的是，实验没有观察到 `bytes/tx` 下降的趋势。代表点上的归一化追踪通信成本普遍上升。这意味着本文最可靠的实证结论并不是“复用已经同时降低了每笔交易的通信代价”，而是“复用确实把一部分原本会被浪费的广播结果重新转化成了后续轮次的吞吐收益”。

== Grace 窗口敏感性

为了进一步解释复用收益与“迟到窗口”之间的关系，本文保留了 Dumbo 路径上的紧凑型敏感性试验。该试验固定 $N=12, f=3$、批处理规模为 32、轮数为 4，并对 `pool_grace_ms \in \{50, 100, 200, 400\}` 分别重复 2 次。

#thesis-table(
  columns: (1.1fr, 1.3fr, 1.3fr, 1.2fr),
  header: ([`pool_grace_ms`], [`TPS_wall` 均值], [`bytes/tx` 均值], [平均复用条目]),
  rows: (
    [50],
    [916.20],
    [682.97],
    [9.0],
    [100],
    [893.49],
    [688.33],
    [9.0],
    [200],
    [955.60],
    [673.95],
    [9.0],
    [400],
    [954.82],
    [675.17],
    [9.0],
  ),
  caption: "Dumbo 路径上 QUIC Grace 窗口敏感性试验结果",
) <tbl-grace-sensitivity>

这组结果呈现出一个对论文很重要的现象：`pool_grace_ms` 并非越大越好。当前正式归档结果中，`50/100/200 ms` 三组的吞吐量和单位交付通信成本都处于相近区间，说明在本地多进程 TCP 环境下，中等范围的 Grace 窗口已经足以截留一批迟到但可复用的广播输出；然而当窗口拉长到 400ms 时，吞吐量明显回落，而单位交付字节数并未同步改善，表明过长等待开始抵消复用带来的收益。换言之，Grace 窗口应理解为一个需要与当前网络条件和实现开销共同调谐的参数，而不是一个可以单调增大的“越保守越好”开关。就当前这组微实验而言，更稳妥的结论不是“100ms 绝对最优”，而是“50 至 200ms 构成了一个较稳健的有效区间，而 400ms 已明显偏大”。

这一组 Grace 结果显示，四个取值下的 `TPS_wall` 都落在比较接近的区间里，复用条目数也基本稳定在 9.0 左右。200ms 和 400ms 略高，100ms 略低，但这种差别还不足以支撑“存在一个明显最优的 Grace 点”这样的结论。在本文本地多进程环境下，50ms 到 400ms 的 Grace 窗口都足以让复用机制保持活跃；参数会影响结果，但更像连续调节，而不是某种清晰的阈值。


== 网络干扰的影响 故障节点的影响 网络与边界场景的补充结果

除了稳定局域网环境外，本文还考察了受控网络扰动。与任务书“诚实但迟到”表述最贴近的是慢诚实节点场景；此外，实验中还纳入了两类最小拜占庭边界场景，用于说明复用与回取链路在运行时边界上的行为。表 \#ref(tbl-slow-honest-quick) 汇总了对应结果。

#thesis-table(
  columns: (1.4fr, 1.1fr, 1.2fr, 1.1fr, 1.2fr),
  header: ([场景], [`TPS_wall` 提升], [`bytes/tx` 变化], [平均复用条目], [平均 fetch 观测]),
  rows: (
    [慢诚实 `none`],
    [+17.23%],
    [+13.83%],
    [21.0],
    [0.0],
    [慢诚实 `80ms`],
    [+19.14%],
    [+13.71%],
    [21.0],
    [0.0],
    [慢诚实 `150ms`],
    [+15.38%],
    [+15.14%],
    [21.0],
    [0.0],
    [静默节点 `silent-p11`],
    [+15.49%],
    [+16.33%],
    [21.0],
    [0.0],
    [损坏 fetch 响应 `invalid_fetch_response-p11`],
    [+15.75%],
    [+13.83%],
    [21.0],
    [0.0],
  ),
  caption: "FIN 风格路径在 QUIC 慢诚实与最小边界故障场景中的结果",
) <tbl-slow-honest-quick>

表中数据至少说明，复用机制并不会因为“部分诚实节点更慢”就完全失效。在 `none`、`80ms` 和 `150ms` 三个慢诚实标签下，`TPS_wall` 依然保持了约 15% 到 19% 的提升。静默节点和损坏 fetch 响应这两类最小边界故障下，复用开启相对关闭也仍有约 15% 左右的改善，说明系统没有因为这些边界注入而出现明显失稳。

同时也要看到，这些场景中的平均 fetch 观测都为 0.0，因此本文不能把这部分结果写成“回取路径已经被正式触发并验证”。正文主实验统一采用 `pool_expire_rounds = 10`，而高负载与边界套件只运行 8 轮，Grace 微实验只运行 4 轮，因而这里的 fetch 主要对应节点局部缓存未命中，而不是条目过期后的强制回取。结合实现逻辑来看，本次实验更像是在观察“缓存命中条件下的跨轮复用收益”，而不是专门测量“缓存未命中时的主动回取收益”。

与慢诚实场景互补，实验平台还支持随机抖动和全局固定延迟两组网络扰动。考虑到这两类扰动更偏向压力测试，而且正文已经有更直接对应任务书语义的慢诚实主证据，本文不在正文中继续堆叠相关表格，而是将其作为后续补充实验方向。

== 结果解释与局限

综合实验结果，核心论点已经能够成立：跨轮次复用确实回收了 ACS 框架下那部分“诚实但迟到”的广播成果，并把它们转化成了后续轮次的吞吐提升。不过，这批结果也要求本文放弃较乐观的通信效率叙述。高负载 QUIC 全扫点里，复用条目数始终稳定在 21.0，`TPS_wall` 也在两条路径上持续上升，这说明复用机制本身确实在发挥作用；但 `tracked_driver_bytes_per_delivered_tx_mean` 没有同步下降，意味着引用、证明以及驱动层追踪到的额外负载，并没有在这组参数下被更高的交付量完全抵消。

因此，本文把结论收在“吞吐恢复”这一层，而不再把它写成“统一降耗”。另外，`tracked_driver_bytes` 本身是一个组合指标，更适合用来做同一实现内部的相对比较，并不适合直接当作物理链路带宽的定量结论。

这一结论仍然有四个边界。其一，结果主要集中在本地回环网络与 $N=12$ 的规模上，尚未覆盖更大节点规模和跨机器部署。其二，通信统计来自驱动层可追踪的若干关键字段，而不是完整的底层网络抓包，因此论文中应明确称其为“追踪通信成本”而非“真实物理链路总字节”。其三，慢诚实和两类最小边界故障仍属于本地受控实验，因此“复用机制对异常网络条件的鲁棒性”不能被表述得过宽。其四，缺失条目回取的实现和指标链路虽然已经接通，但在当前这批正文结果中尚未成为主要收益来源，因此更适合作为系统边界与后续实验入口，而不是当前论文的主结论支柱。

= 总结与展望

// 约 1000 字

== 本课题主要的成果

本课题针对基于 ACS 的异步 BFT 共识协议广播阶段诚实但迟到节点不能进入共识阶段造成带宽浪费的问题，设计了一种可行有效的缓存复用机制，通过将迟到数据缓存并通过轻量级索引再下一轮次重新发起提案，减少了带宽浪费并提高了协议的吞吐量。该机制不直接修改 ACS 黑盒，保留了经典的 ACS 结构，方便移植到现有的程序上，

== 展望：偏序关系的维护与广播数据浪费的问题
值得一提的是，无论是流水线的水位线，还是 DAG 维护 partial order，其实都维护着一种 partial order，而严格串行的结构实际上（如果不做比如internodelinking 的设计，就会很自然的抛弃这些信息，因而实际上只要完全并行广播和共识，就不会出现我们后面提到的带宽浪费问题）

若一个异步BFT协议完全并行了广播与共识，则它必然已经在维护某种偏序关系，而维护偏序关系本身就隐含地解决了"诚实但迟到的广播被浪费"这个问题。因此跨轮复用机制对完全并行的协议是冗余的。

"完全并行广播与共识"意味着共识层在推进决定时，不需要等待当前轮广播全部完成。这只有一种实现方式：共识层必须能够追踪"哪些广播已经可用"这个动态信息，并据此推进。而追踪这个信息，本质上就是在维护一个偏序——无论表现为DAG的边、水位线的前缀还是V向量。

反过来说，一旦放弃维护偏序、退回到严格串行结构，广播浪费问题就必然重新出现，因为系统不再有任何机制去"记住"那些迟到的广播。

在不引入偏序维护的前提下，用最小代价回收一部分本会被丢弃的广播结果。它是严格串行ACS为了回避偏序维护复杂性而付出的代价的局部补偿，而不是对完全并行方案的竞争。

本文机制专注于严格串行ACS框架内的增量优化。从更宏观的视角看，完全并行广播与共识的方案（如DumboNG、DAG-based BFT）在协议层面已经隐含地维护了某种偏序关系，这从根本上避免了广播浪费问题的出现。因此本文机制对这类协议是冗余的。这一观察提示，严格串行ACS与完全并行方案之间的本质差异，可能不只是性能工程上的取舍，而是对偏序信息保留程度的根本性选择。对这一关系的严格形式化论证留待后续工作。

本文围绕 ACS 架构中的广播浪费问题，给出了一套跨轮次复用方案，并完成了相应原型实现。问题来自宿主层对迟到广播输出的丢弃，方案落在统一提案编码、引用绑定、缓存管理和缺失数据回取上。实验结果表明，这条主线能够从机制设计落实到可观测的吞吐收益。

从实验结果看，本文最稳固的结论是：跨轮次复用确实能够把一部分原本会被浪费的历史广播成果转化成后续轮次的吞吐收益。高负载 QUIC 主结果里，Dumbo 路径在全部批处理点上都保持正收益，FIN 风格路径在全部扫点上的 `TPS_wall` 提升都超过了 16%，已经满足任务书对高负载吞吐改进的核心要求。与此同时，本文也必须承认一个不那么理想但更真实的事实：在本文实现与参数下，吞吐收益并没有自动伴随每笔交易追踪通信成本的下降。因此，本文不能把结论写得过满，更不能据此推出“复用已经全面降低通信开销”。

如果把本文的实际工作压缩成一句话，那么它完成的是这样一件事：在不改动 ACS 决定语义的前提下，把原本会在当前轮被丢弃的那部分诚实广播结果保留下来，并在后续轮次中继续利用。实验表明，这一方向的收益更明确地体现在吞吐恢复上，而不是统一的通信降耗上。

后续仍值得继续做的工作，主要是进一步拓展实验范围。最重要的是补一次跨机器最小复现，其次是把固定延迟和随机抖动结果整理成附录材料，再往后则是补出一个能够稳定触发 fetch 的实验场景。只要这几项补充能够完成，本文就可以保持比较清楚的边界：它不是在宣称一种适用于任意异步共识架构的通用加速方法，而是在 HoneyBadger/Dumbo 一类 ACS 协议上，对一个长期存在但常被忽略的工程浪费点给出了一种可实现、可测量、也有实证支持的改进方案。
